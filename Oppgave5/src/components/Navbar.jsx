import React from 'react';
import Title from './Title';

const Navbar = ({ title }) => (
    

        <header>
           <Title title={title}/>  
            <nav>
                <h2>User User</h2>
            </nav>
        </header>

   
    );

export default Navbar;